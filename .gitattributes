# Treat all text files as text and ensure they end in LF
* text=auto

# Explicitly set line endings for certain file types
*.py text eol=lf
*.sh text eol=lf
*.bat text eol=crlf
*.ps1 text eol=crlf
*.html text eol=lf
# Ignore binary files (e.g., images)
*.png binary
*.jpg binary
s